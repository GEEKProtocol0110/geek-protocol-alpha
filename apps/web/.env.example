# =============================================================
# GEEK PROTOCOL WEB APP – ENVIRONMENT TEMPLATE
# =============================================================
# Copy this file to apps/web/.env.local (or .env.development) and
# customize values for your deployment target.

# Base URL for API requests. This must match the Fastify API origin.
NEXT_PUBLIC_API_URL="http://localhost:3002"

# Reward stub for the App Router API route (apps/web/src/app/api/...)
# The real reward calculation happens server-side once wired to Kaspa.
REWARD_PER_CORRECT="10"

# Optional: public app URL for canonical links and OG tags
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
